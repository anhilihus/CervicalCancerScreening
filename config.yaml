# Configuration for Cervical Cell Segmentation
# Based on Capstone Project Report

paths:
  input_dir: "Image_Dataset"
  output_dir: "output_results"

thresholds:
  dark:
    min: 50
    max: 100
  medium:
    min: 100
    max: 150

morphology:
  kernel_size: 3  # For (3,3) kernel
  iterations: 1

visualization:
  save_plots: true
  plot_format: "png"
